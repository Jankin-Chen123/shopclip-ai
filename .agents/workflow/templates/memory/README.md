# Local Memory Templates

These files are templates for local user memory.

Copy them into `.agents/memory/` when local memory is needed. The `.agents/memory/` directory is ignored by Git and must not be committed.
